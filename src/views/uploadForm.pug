doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Upload Form
    link(rel="stylesheet", href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css")
    style.
      body {
        background-color: #f8f9fa;
      }
      h1 {
        text-align: center;
        margin-bottom: 30px;
      }
      .form-container {
        max-width: 500px;
        margin: 50px auto;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(0,0,0,0.1);
      }
      label {
        font-weight: bold;
      }
      input[type="text"] {
        border: 1px solid #ced4da;
        border-radius: 5px;
        padding: 10px;
        width: 100%;
        box-sizing: border-box;
      }
      input[type="file"] {
        border: 1px solid #ced4da;
        border-radius: 5px;
        padding: 10px;
        width: 100%;
        height: 3.3rem;
        box-sizing: border-box;
      }
      button[type="submit"] {
        width: 30%;
        height: 2.5rem;
        font-size: 1rem;
      }
      button[type="submit"]:hover {
        background-color: #007bff;
      }
  body
    .container
      .row.justify-content-center
        .col-lg-6.form-container
          h1 Upload File
          form(action=`/${username}/uploadList/uploadFile`, method="post", enctype="multipart/form-data")
            .form-group
              label(for="searchTerms") Search Terms (comma separated):
              input.form-control(type="text", id="searchTerms", name="searchTerms", placeholder="e.g., term1, term2, term3" required)
            .form-group
              label(for="file") Upload File:
              input.form-control(type="file", id="file", name="file", accept=".jpg, .jpeg, .png, .gif" required)
            button.btn.btn-primary.btn-sm(type="submit") Submit
